<html>
    <header>
    </header>
    <body>
        <table>
            <tr>
                <th>index</th>
                <th>value</th>
            </tr>
            <% ret.forEach(function(value, index){ %>
                <tr>
                    <td><%= index %></td>
                    <td><%= value %></td>
                </tr>
            <% }); %>
        </table>
    </body>
</html>
